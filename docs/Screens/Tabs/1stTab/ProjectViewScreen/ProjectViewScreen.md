# Экран просмотра проекта

## Когда показывается
Открывается при выборе проекта из списка на экране **«Проекты»**.

## Назначение
Показать подробную информацию о выбранном проекте: основные данные, статус, описание, требования и команду.

## Структура экрана

### 1) Шапка
- Кнопка **назад** (стрелка).
- Заголовок: **«Детали проекта»**.
- Индикатор авторизации **«🔐 Авторизован»** (если пользователь авторизован).

### 2) Карточка проекта
Содержит ключевые данные:
- Дата (слева).
- Название проекта.
- Иконка обновления/перезагрузки данных.
- Плашки/поля с датами:
  - **Срок записи на проект**
  - **Срок реализации проекта**
- Информация о заказчике.

### 3) Дополнительная информация
- Контакты/заказчик (в виде небольших плашек).
- Теги/стек проекта (в виде чипсов).
- Статус: например: **«Назначена команда»**.

### 4) Описание проекта
- Заголовок: **«Описание проекта»**
- Текстовое описание проекта.

### 5) Требования проекта
- Заголовок: **«Требования проекта»**
- Нумерованный список требований.

### 6) Требования для исполнителей
- Заголовок: **«Требования для исполнителей»**
- Чипсы с навыками/требованиями.

### 7) Команда
- Карточка **«Команда»** со списком участников и их ролями.

## Выпадающее меню действий
Справа внизу — группа кнопок:
- **GitHub**
- **Статистика проекта**

## Ограничение для неавторизованного пользователя
Если пользователь **не авторизован**, он видит информацию **только до блока «Требования проекта» (не включительно)** — то есть карточку проекта, статус и **«Описание проекта»**. Всё, что начинается с **«Требования проекта»** и ниже, скрыто.

## Техническая реализация

### API запрос

#### Получение детальной информации о проекте

**Эндпоинт:** `GET /project/{slug}`

**Параметры:**
- `slug` — уникальный идентификатор проекта (строка)

**Ответ:**
```json
{
  "project": {
    "id": "string",
    "name": "string",
    "description": "string",
    "shortDescription": "string",
    "dateStart": "string",
    "dateEnd": "string",
    "slug": "string",
    "tags": ["string"],
    "team": "string",
    "status": "string"
  },
  "tags": [
    {
      "id": "string",
      "name": "string",
      "description": "string"
    }
  ],
  "teams": [
    {
      "id": "string",
      "name": "string",
      "description": "string"
    }
  ],
  "members": [
    {
      "id": "string",
      "name": "string",
      "role": "string"
    }
  ],
  "users": [
    {
      "id": "string",
      "username": "string",
      "email": "string"
    }
  ]
}
```

**Реализация:**
- Файл: `composeApp/src/commonMain/kotlin/com/spbu/projecttrack/projects/data/api/ProjectsApi.kt`
- Метод: `getProjectById(id: String)`
- Репозиторий: `ProjectsRepository.getProjectById(id)`

### Состояния экрана

**ProjectDetailUiState:**
- `Loading` — загрузка данных
- `Success(project, tags, teams, members, users)` — успешная загрузка
- `Error(message)` — ошибка загрузки

**Реализация:**
- ViewModel: `ProjectDetailViewModel.kt`
- Инициализация: загрузка данных при создании ViewModel
- Retry: метод `retry()` для повторной загрузки при ошибке

### Модели данных

**ProjectDetail:**
```kotlin
data class ProjectDetail(
    val id: String,
    val name: String,
    val description: String? = null,
    val shortDescription: String? = null,
    val dateStart: String? = null,
    val dateEnd: String? = null,
    val slug: String? = null,
    val tags: List<String>? = null,
    val team: String? = null,
    val status: String? = null
)
```

**Tag:**
```kotlin
data class Tag(
    val id: String,
    val name: String,
    val description: String? = null
)
```

**Team:**
```kotlin
data class Team(
    val id: String,
    val name: String,
    val description: String? = null
)
```

**Member:**
```kotlin
data class Member(
    val id: String,
    val name: String,
    val role: String? = null
)
```

**User:**
```kotlin
data class User(
    val id: String,
    val username: String? = null,
    val email: String? = null
)
```

### Проверка авторизации

Проверка авторизации выполняется через `AuthManager.isAuthorized`:
- Если пользователь авторизован — показывается полная информация
- Если не авторизован — скрываются блоки "Требования проекта" и ниже

**Реализация:**
- Файл: `ProjectDetailScreen.kt`
- Проверка: `val isAuthorized by AuthManager.isAuthorized.collectAsState()`

### Архитектура

**Слои:**
1. **UI:** `ProjectDetailScreen.kt` — Compose UI
2. **ViewModel:** `ProjectDetailViewModel.kt` — управление состоянием
3. **Repository:** `ProjectsRepository.kt` — абстракция данных
4. **API:** `ProjectsApi.kt` — HTTP запросы

**Поток данных:**
```
ProjectDetailScreen → ProjectDetailViewModel → ProjectsRepository → ProjectsApi → HTTP
```

### Навигация

- Кнопка "Назад" возвращает на экран списка проектов
- Передача `projectId` (slug) через параметры навигации