package com.spbu.projecttrack.core.theme

import androidx.compose.ui.graphics.Color

object AppColors {
    val Color1 = Color(0xFFBDBDBD) // Светло-серый (border)
    val Color2 = Color(0xFF76767C) // Темно-серый (основной текст)
    val Color3 = Color(0xFF9F2D20) // Бордовый (акцент, титулы)
    val BorderColor = Color(0xFFCF3F2F) // Для обводки кнопок
    val White = Color(0xFFFFFFFF)
    val Black = Color(0xFF000000)
    
    // Градиенты для карточек проекта
    val GradientStart = Color(0xFF9F2D20) // Верх градиента
    val GradientEnd = Color(0xFF792117) // Низ градиента (более тёмный)
    val GradientEndAlt = Color(0xFF792016) // Альтернативный низ градиента
    
    // Для карточек и теней
    val CardBorder = Color(0xFFF3F3F3) // Граница карточки
    val CardShadow = Color(0x26000000) // Тень карточки (15% чёрного)
    val DarkShadow = Color(0x3F000000) // Тень для градиентных блоков (25% чёрного)
    
    // Для команды блока
    val TeamGradientStart = Color(0xFF9F2D20)
    val TeamGradientEnd = Color(0xFF7C2015)
    val TeamBorder = Color(0xFFCF3F2F)
}









